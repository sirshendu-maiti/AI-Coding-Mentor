<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: enforce HTTPS for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug: allow cleartext for local backend testing (emulator localhost) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Allow cleartext to emulator localhost for dev server & Firebase emulator -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Allow HTTPS to Firebase Cloud Functions -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">cloudfunctions.net</domain>
    </domain-config>
</network-security-config>
